# Runtime
PyYAML>=6.0
pydantic>=2.7,<3
openai>=1.40                 # tested on 1.108 (local) and 3.6 (image); parse() is stable
langgraph>=1.0,<2
fastapi>=0.111
uvicorn>=0.30
# observability uses the stdlib logging module — no dependency

# Dev / test
pytest>=8.0
httpx>=0.27                  # FastAPI TestClient
