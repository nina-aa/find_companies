# Runtime
PyYAML>=6.0
pydantic>=2.7          # tool contracts (M2), LLM response schemas (M3)

# --- added in later milestones ---
# openai>=1.40         (M3 — LLM client)
# structlog>=24.1      (M4 — observability)
# fastapi>=0.111       (M4 — HTTP API)
# uvicorn>=0.30        (M4 — HTTP API)

# Dev / test
pytest>=8.0
