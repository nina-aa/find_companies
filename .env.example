# Copy to .env and fill in. .env is git-ignored.

# --- LLM (added in M3) ---
OPENAI_API_KEY=sk-...
LLM_MODEL=gpt-4o-mini
LLM_PROVIDER=openai            # openai | fake

# --- API (added in M4) ---
AGENT_API_KEY=change-me        # X-API-Key shared secret for the HTTP endpoint
AGENT_DEADLINE_S=90            # per-run wall-clock deadline (BudgetGuard)
